import UsersList from "../../components/UsersList";

type Props = {};
const Dashboard = (props: Props) => {
  return (
    <div>
      Dashboard Index
      <UsersList />
    </div>
  );
};
export default Dashboard;
